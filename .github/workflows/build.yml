name: Build FocusFlow APK

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: gradle

    - name: Setup Android SDK
      uses: android-actions/setup-android@v2
      with:
        api-level: 37
        build-tools-version: '37.0.0'
        ndk-version: 'r26'

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build with Gradle
      run: ./gradlew build --stacktrace

    - name: Build Release APK
      run: ./gradlew assembleRelease -x test --stacktrace

    - name: Build Bundle
      run: ./gradlew bundleRelease -x test --stacktrace

    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: app-release-apk
        path: app/build/outputs/apk/release/app-release.apk
        retention-days: 30

    - name: Upload Bundle
      uses: actions/upload-artifact@v3
      with:
        name: app-release-bundle
        path: app/build/outputs/bundle/release/app-release.aab
        retention-days: 30

    - name: Sign APK
      if: always()
      run: |
        echo "APK built successfully at: app/build/outputs/apk/release/"
        ls -lah app/build/outputs/

    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        body: 'FocusFlow Release Build'
        draft: false
        prerelease: false
