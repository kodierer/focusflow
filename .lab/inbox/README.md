# Inbox

Pfad: `iter-{NN}-{role}-{kurzslug}.md`

Pflicht-YAML-Kopf: id, iteration, role, persona (falls Tester), parent, priority, status, input_files, output_schema, output_file, budget_hint.

Body: Ziel, Kontext, Verbote, Definition of Done.

Fertig → Status `done` oder nach `inbox/_done/` verschieben.
